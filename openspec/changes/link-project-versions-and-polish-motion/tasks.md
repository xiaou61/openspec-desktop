## 1. 版本契约与目录迁移

- [ ] 1.1 在共享契约中增加版本模式、版本来源和项目版本上下文字段，并保持 `versionLabel` 作为有效历史快照值
- [ ] 1.2 定义版本摘要、版本筛选和版本刷新所需的共享响应与请求 schema
- [ ] 1.3 将目录 schema 升级到 v2，并实现保留项目、分组、偏好和窗口状态的 v1→v2 纯数据迁移
- [ ] 1.4 在目录加载器中加入迁移前备份、原子保存和可恢复的中文错误路径，避免把可识别旧目录当作损坏数据清空
- [ ] 1.5 为非空旧版本、空旧版本、完整目录保留和迁移失败恢复增加目录存储测试
- [ ] 1.6 更新共享契约夹具和序列化测试，覆盖所有版本来源及 120 字符边界

## 2. 本地版本解析

- [ ] 2.1 新建主进程版本解析模块，并定义可注入文件系统与 Git 执行依赖的有界接口
- [ ] 2.2 使用无 shell 的 `execFile` 读取当前 HEAD 精确标签，限制超时、输出大小并按 Git 版本顺序确定多标签结果
- [ ] 2.3 使用有大小上限的 JSON 读取实现根目录 `package.json` 版本回退，并校验空白、类型和长度
- [ ] 2.4 实现 Git 标签、包版本与当前工作区的确定性优先级及安全诊断结果
- [ ] 2.5 为 Git 标签优先、多标签、非仓库、Git 缺失、超时、恶意路径、包文件损坏和无版本场景增加单元测试

## 3. 项目生命周期与监听上下文

- [ ] 3.1 扩展目录服务的项目注册和更新接口，支持自动/手动模式、来源和解析时间的原子写入
- [ ] 3.2 在应用初始化、手动添加、Codex 导入、重新定位、重新扫描和显式刷新中接入统一的自动版本协调流程
- [ ] 3.3 保证手动模式跳过自动覆盖，并在恢复自动模式时立即重新解析本地线索
- [ ] 3.4 为 WatcherManager 增加可替换的最新项目上下文，使每次历史写入读取当前 `versionLabel`
- [ ] 3.5 在版本标签、模式或来源变化时记录一次项目设置活动，并抑制解析结果不变产生的活动噪声
- [ ] 3.6 为设置请求返回后的版本传播、监听不中断、无重复修订和旧记录不回写增加 WatcherManager 与 AppController 测试
- [ ] 3.7 为 Codex 导入自动解析、Git 失败仍可导入和手动版本保留增加集成测试

## 4. 历史聚合与受限 API

- [ ] 4.1 在 HistoryStore 中按版本标签聚合活动数、修订数、时间范围和去重 Change 集合
- [ ] 4.2 为活动与修订查询增加版本筛选，并确保过滤发生在游标分页切片之前
- [ ] 4.3 将空版本稳定映射为内部 `workspace` 键和中文“当前工作区”展示标签
- [ ] 4.4 扩展 AppController、IPC 路由、preload 与 DesktopApi，提供版本摘要、版本刷新和筛选查询
- [ ] 4.5 对所有新增 IPC 输入执行严格 schema 校验，并保持渲染进程无文件系统与命令执行能力
- [ ] 4.6 为同名版本合并、Change 跨版本、无关联历史、空版本、分页过滤和 IPC 拒绝非法输入增加测试

## 5. 版本关联界面

- [ ] 5.1 将项目导航中的空版本文案替换为“当前工作区”，并为长标签增加稳定宽度、截断和完整可访问名称
- [ ] 5.2 在项目标题区实现紧凑版本触发器与 Radix 下拉菜单，显示标签、来源、刷新和进入设置操作
- [ ] 5.3 将项目设置中的版本编辑改为自动/手动分段模式，加入手动标签就地校验、保存状态和恢复自动操作
- [ ] 5.4 查询项目版本摘要，并在 Change 行显示最近关联版本、跨版本数量或“尚无版本活动”
- [ ] 5.5 在 Change 详情中展示可操作的完整关联版本集合，点击版本可切换当前活动筛选
- [ ] 5.6 将活动时间线改为低噪声版本分组，保留每条记录的时间、摘要、文档和任务变化信息
- [ ] 5.7 为活动和修订工具栏增加共享版本筛选器、带语境空状态和返回全部版本操作
- [ ] 5.8 为版本加载、刷新成功、解析回退和保存失败加入中文可感知反馈与实时区域通知
- [ ] 5.9 更新 React Query 缓存失效规则，使版本、项目快照、活动和修订在实时事件后保持一致
- [ ] 5.10 增加渲染测试，覆盖键盘操作、焦点返回、自动/手动切换、分组、筛选、跨版本和长标签

## 6. 动效与视觉精修

- [ ] 6.1 在样式根建立共享动效时长和缓动令牌，并清除普通 UI 中的任意长时动画、ease-in 与 `transition: all`
- [ ] 6.2 为按钮、图标按钮、项目行、Change 行和分段控件增加即时按压反馈，并将悬停位移限制在精确指针环境
- [ ] 6.3 使用 Radix `data-state` 和触发点变换原点实现版本菜单的可中断进入退出
- [ ] 6.4 将项目设置和现有浮层统一到居中对话框过渡，协调遮罩淡入、焦点捕获与关闭返回
- [ ] 6.5 精修详情标签与分段控件指示器、内容淡入和实时列表进入反馈，保证快速反向操作不跳帧
- [ ] 6.6 将进度条变化改为固定轨道内的 `scaleX` 过渡，消除宽度动画导致的布局与绘制开销
- [ ] 6.7 收敛玻璃材质到导航、工具栏和浮层，为正文和密集列表保留高对比实色表面与细分隔线
- [ ] 6.8 完成背景模糊不支持、减少动态效果、减少透明度和更高对比度的集中降级规则
- [ ] 6.9 校正最小窗口与宽屏布局中版本控件、长 Change 名称、浮层边界和滚动区域，确保无重叠和文本溢出

## 7. 验证与交付

- [ ] 7.1 运行类型检查、Lint、单元测试和渲染边界检查并修复全部回归
- [ ] 7.2 运行 Chromium 工作区流程，覆盖版本刷新、手动覆盖、版本筛选与减少动态效果
- [ ] 7.3 运行真实 Electron 流程，验证 Git/包版本解析、Codex 导入、监听中的版本切换和历史关联
- [ ] 7.4 在 1440×900、最小支持窗口和宽屏视口采集截图，检查非空画面、玻璃降级、长文本、菜单/对话框定位和控件重叠
- [ ] 7.5 使用浏览器动画检查与快速反向操作检查菜单、对话框、标签和列表过渡，确认普通动效不超过 300 毫秒且减少动态效果有效
- [ ] 7.6 更新中文 README 中的版本上下文、自动来源、历史关联和只读边界说明
- [ ] 7.7 运行完整构建与 Windows 安装版/便携版打包，记录产物大小和 SHA-256
- [ ] 7.8 严格校验 OpenSpec 变更，并逐项核对实现与两份能力规格一致
