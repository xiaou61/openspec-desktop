## 1. 基线与边界核对

- [x] 1.1 使用 CodeGraph 重新核对 `App.tsx`、`lifecycle-view.tsx`、通知、侧栏和样式入口的当前调用与状态流，记录与进行中生命周期变更的重叠点
- [x] 1.2 在常用桌面窗口和应用声明的最小窗口保存当前界面基线，覆盖项目列表、Change 列表、生命周期和就绪检查
- [x] 1.3 建立列表、详情标题与生命周期的状态术语和语义样式映射，标出可合并的重复徽标、进度和时间信息
- [x] 1.4 确认本变更不触碰主进程生命周期判定、IPC 契约、持久化数据和 OpenSpec 项目文件写入边界

## 2. 视觉与样式基础

- [x] 2.1 建立画布、导航、内容、浮层、边界、文字、焦点和状态的分层语义令牌，并移除本次触及区域中的重复近似值
- [x] 2.2 建立固定字号、行高、间距、控件尺寸、圆角和阴影令牌，确保紧凑面板标题不使用页面级字号且字间距为零
- [x] 2.3 建立按压、选择、小型状态、展开、浮层、通知和退出的共享动效时长与缓动令牌
- [x] 2.4 将本次修改的样式按令牌、工作区、组件、生命周期、动效和响应式职责拆分，并保持稳定导入与级联顺序
- [x] 2.5 删除迁移后的旧规则、局部任意动效值和 `transition: all`，确保同一选择器不存在新旧定义竞争
- [x] 2.6 完成背景模糊不支持、减少透明度和高对比模式下的实色表面、边界与焦点降级

## 3. 三栏工作区视觉重构

- [x] 3.1 调整三栏 Grid 的有界宽度与收缩规则，让详情获得主要空间且最小窗口不产生页面级横向滚动
- [x] 3.2 压缩应用标题、面板标题和详情标题的无效高度，建立名称、主要状态、版本与时间的两级信息结构
- [x] 3.3 重构项目栏的分组、计数、操作入口和选择态，使用稳定选择轨与轻表面替代浮起卡片效果
- [x] 3.4 重构 Change 行的信息顺序与密度，合并重复进度表达并保持名称、状态、任务和时间稳定对齐
- [x] 3.5 重构详情标题和标签导航，使长 Change 名称可受限换行且不会挤走状态、标签或主要命令
- [x] 3.6 统一按钮、图标按钮、分段控件、徽标、进度条、菜单、通知和对话框的尺寸、边界与交互状态
- [x] 3.7 为长中文、能力名、版本标签和连续文件路径补齐换行、截断、完整值提示与 `min-width: 0` 约束

## 4. 生命周期与就绪信息编排

- [x] 4.1 为详情区域建立容器查询边界，并以紧凑布局作为容器查询不可用时的安全默认值
- [x] 4.2 移除生命周期轨道的固定最小宽度和横向滚动依赖，实现宽版与紧凑版六节点布局
- [x] 4.3 重构生命周期节点、连接、当前态、阻塞态与完成态的语义外观，同时保留完整可访问名称
- [x] 4.4 重构建议下一步、工件关系、验证、归档检查与只读规格影响的标题、摘要、计数和证据层级
- [x] 4.5 将归档说明中的规格影响计数和能力详情改为便于比较的紧凑结构，减少嵌套卡片与重复说明
- [x] 4.6 核对列表、详情标题和生命周期中相同状态的术语、图标与颜色完全一致

## 5. 有目的的交互动效

- [x] 5.1 实现带 `aria-expanded` 与关联区域的受控披露组件，并覆盖打开、关闭、快速反向和键盘行为
- [x] 5.2 用受控披露替换规格影响能力与检查证据的突然展开，使用 160 至 220 毫秒的网格轨道与透明度过渡
- [x] 5.3 为生命周期节点和连接实现一次性状态过渡，保持节点位置、焦点和滚动上下文不变
- [x] 5.4 重构验证操作的空闲、运行、成功、失败、不可用和过期反馈，保持按钮宽度与相邻布局稳定
- [x] 5.5 为通知增加 `entering/open/closing` 存在状态、集中定时器清理和可立即触发的 240/160 毫秒进入退出
- [x] 5.6 协调窄屏项目侧栏与遮罩的进入退出、Escape、遮罩点击、选择关闭和焦点返回
- [x] 5.7 统一项目、Change、按钮与分段控件的按压和选择反馈，移除列表行明显上浮、弹跳与触摸环境伪悬停
- [x] 5.8 将标签内容变化限制为不超过 160 毫秒的轻透明度反馈，禁止实时列表整组重播和静止状态循环装饰动画

## 6. 无障碍、响应式与性能收口

- [x] 6.1 核对三栏、生命周期、披露、通知、对话框和窄屏侧栏的焦点顺序、可见焦点、可访问名称与关闭后焦点返回
- [x] 6.2 实现减少动态效果下的即时状态切换，移除位移、缩放、展开和循环动画，同时保留文字、图标与边界反馈
- [x] 6.3 核对常用窗口、最小窗口和长内容状态下不存在文字遮挡、控件位移、页面级水平滚动或不可达命令
- [x] 6.4 检查所有预定动效只改变必要属性、可被新输入中断，并在静止后不保留无用计时器或循环任务
- [x] 6.5 若新增或重命名跨文件导出，运行 `codegraph index --force` 更新索引并复核受影响调用路径

## 7. 针对性验证与交付

- [x] 7.1 更新渲染层测试，覆盖状态去重、长内容、紧凑生命周期、披露键盘语义、验证稳定反馈、通知退出和减少动态效果分支
- [x] 7.2 运行 `pnpm typecheck`、`pnpm lint`、`pnpm test:renderer` 和 `pnpm build`，修复本变更引入的错误
- [x] 7.3 在常用桌面窗口与最小窗口核对项目、Change、文档、生命周期和就绪页面，并保存改造后截图与基线对比
- [x] 7.4 审查所有动画时长、缓动、触发频率和静止状态，确认只保留规格列出的六类动效且无明显卡顿
- [x] 7.5 更新中文 README 或版本说明中的界面变化与系统偏好降级说明，不加入使用教程式界面文案
- [x] 7.6 运行 `openspec validate refine-desktop-visual-system-and-motion --strict` 并核对任务完成状态
- [x] 7.7 运行 `pnpm package:win` 生成安装版与便携版并记录文件大小和 SHA-256；不运行完整 Electron 冒烟套件
