## 1. 共享契约与目录迁移

- [x] 1.1 在共享契约中定义直接项目、工作区、OpenSpec 成员、未配置仓库和局部诊断的可判别联合，并为每层数组、文本和计数设置上限
- [x] 1.2 扩展 Codex 列表摘要，分别返回索引根、工作区、代码仓库、OpenSpec 项目和可导入项目数量，同时保留来源、截断和降级信息
- [x] 1.3 扩展导入请求和逐项结果，使可导入叶子携带可选工作区身份、根路径和显示名，并用严格契约拒绝缺字段、越界和超量载荷
- [x] 1.4 将 `ProjectGroup` 定义为 `manual` 与 `codex-workspace` 联合，后者要求规范化 `sourceRootPath`，并把目录状态版本升级到 v3
- [x] 1.5 实现 catalog v2 到 v3 迁移和迁移前备份，保持分组 ID、顺序、项目 `groupId`、项目顺序及偏好不变
- [x] 1.6 补充共享契约和目录存储测试，覆盖合法联合、非法工作区成员、计数上限、v2 迁移、损坏 v3 恢复及备份失败

## 2. 有界工作区成员发现

- [x] 2.1 为工作区发现定义可注入的深度、目录数、成员数、并发度和软截止时间限制，以及默认排除目录与项目标记集合
- [x] 2.2 实现精确根分类，使有效 OpenSpec 根直接返回项目，不可读取根返回诊断，只有可读取但非 OpenSpec 的目录进入成员发现
- [x] 2.3 实现广度优先目录遍历，使用 `lstat` 和目录项类型跳过符号链接、Windows 目录联接、依赖目录、版本控制内容和构建输出
- [x] 2.4 实现 OpenSpec 根识别与短路，发现有效项目后产出单个成员且不继续扫描其后代
- [x] 2.5 实现 `.git` 文件或目录及受支持清单文件的代码仓库识别，将无 OpenSpec 仓库标记为 `not-configured`，并忽略普通资料目录
- [x] 2.6 实现目录、成员和软时间预算，达到任一限制后停止调度新读取、保留已发现结果并返回具体截断原因
- [x] 2.7 隔离单个不可读取或扫描期间消失的成员错误，使其形成局部诊断而不丢弃同一工作区的其他结果
- [x] 2.8 建立临时目录测试样本，覆盖演示三仓库工作区、直接 OpenSpec 根、两层嵌套、排除目录、符号链接、成员消失、预算截断和无项目容器

## 3. Codex 来源归并与稳定去重

- [x] 3.1 把现有 Codex 索引解析接入精确根分类和工作区发现，同时保持主状态文件重试、只读备份回退及隐私字段隔离行为
- [x] 3.2 使用现有平台路径规范化和可用时的真实路径生成稳定项目与工作区键，统一 Windows 大小写、尾分隔符和扩展路径前缀
- [x] 3.3 在全部来源收集后合并直接索引项与嵌套成员，使每个项目只保留一个叶子身份并保留最早 Codex 排序信息
- [x] 3.4 实现重叠工作区归属规则，优先选择最近祖先，距离相同时使用 Codex 索引顺序，并用测试固定确定性结果
- [x] 3.5 在归并后应用已注册项目状态并计算各层摘要，确保工作区父目录和未配置仓库不进入可导入数量
- [x] 3.6 扩展 Codex 发现单元测试，覆盖子项目同时直接索引、多个父容器覆盖、已添加子项目、同名不同路径工作区及全局候选截断

## 4. 自动工作区分组与目录服务

- [x] 4.1 在目录服务中增加按规范化 `sourceRootPath` 查找自动工作区分组的能力，且不按名称匹配手工分组
- [x] 4.2 增加串行的“注册项目并关联工作区”复合操作，在项目验证成功后创建或复用分组，避免失败导入遗留空分组
- [x] 4.3 确保同一工作区后续导入复用原分组、保留用户重命名后的名称，并防止并发请求创建重复来源分组
- [x] 4.4 保持已注册项目的现有分组归属，不因重新发现自动移动项目，并保持移除自动工作区分组时仅解除关联
- [x] 4.5 更新手工分组创建、修改和删除流程，使其显式使用 `manual` 类型且行为与升级前一致
- [x] 4.6 扩展目录服务和存储测试，覆盖首次导入、同源复用、同名手工分组、失败无空组、已分组项目、重命名和移除分组

## 5. 控制器、IPC 与 preload 集成

- [x] 5.1 更新应用控制器的 Codex 列表接口，返回分层发现结果并继续只向渲染进程暴露必要的本机路径和状态字段
- [x] 5.2 在确认导入时重新规范化子项目和工作区路径，验证子项目位于声明工作区内并重新执行精确 OpenSpec 校验
- [x] 5.3 将直接项目导入继续路由到原注册流程，将工作区子项目路由到自动分组复合操作，且永不注册或监控父目录
- [x] 5.4 保留批量导入逐项成功语义，返回工作区关联、已添加和失败结果，并确保一个失效成员不回滚其他成功成员
- [x] 5.5 更新 IPC 路由、桌面 API 和 preload 校验，使新联合响应和导入载荷在主进程与渲染进程之间保持一致
- [x] 5.6 补充控制器、IPC 和 preload 测试，覆盖伪造父子关系、路径逃逸、确认时 OpenSpec 失效、部分成功、重复请求和直接项目兼容

## 6. 分层导入界面与主侧边栏

- [x] 6.1 抽取分层候选的可导入叶子、计数和选择派生函数，并用纯函数测试覆盖直接项目、多个工作区和空工作区
- [x] 6.2 重构 Codex 导入列表，使用可展开工作区容器行和稳定成员行展示名称、路径、类型、仓库数、OpenSpec 数及文字状态
- [x] 6.3 实现工作区父复选框的未选、部分选择和全选状态，只影响 `available` 子项目，并保持全局“全选可用项目”语义
- [x] 6.4 在展开、折叠和刷新时按项目路径身份保留仍有效的选择，移除已经消失或变为不可导入的选择并同步底部计数
- [x] 6.5 更新顶部摘要、空态和错误态，区分 Codex 根目录、工作区、OpenSpec 项目和可导入数量，并在工作区及总体层展示截断或局部诊断
- [x] 6.6 为未配置 OpenSpec、已添加、不可读取和无项目结构提供明确禁用文案，移除容器工作区上的“不是 OpenSpec 项目”误导标签
- [x] 6.7 在主侧边栏为 `codex-workspace` 分组增加工作区标识和来源路径 Tooltip，同时保持子项目选择、Change 列表和监控状态独立
- [x] 6.8 完成展开按钮、三态复选框、焦点顺序、`aria-expanded`、可访问名称、长路径省略和减少动态效果处理
- [x] 6.9 扩展 React 组件测试，覆盖演示工作区列表、键盘展开、父子选择、折叠后提交、刷新协调、诊断提示、直接导入和侧边栏分组

## 7. 集成验证与 Windows 交付

- [x] 7.1 新增跨文件契约和导出后运行 `codegraph index --force .`，再用定向查询核对发现、导入、分组、持久化和渲染调用链
- [x] 7.2 运行格式检查、TypeScript 类型检查、ESLint、全部 Vitest、渲染边界检查和生产构建，并修复所有回归
- [x] 7.3 扩展 Playwright 浏览器流程，使用隔离 fixture 验证工作区展开、准确计数、三态选择、部分失败和直接项目兼容
- [x] 7.4 扩展 Electron E2E，在 1440x900 与 920x700 窗口验证真实 IPC 导入、自动分组持久化、重启恢复、键盘操作和无重叠截图
- [x] 7.5 对脱敏演示工作区执行只读人工验收，确认识别 3 个代码仓库、仅 1 个子项目可导入、父目录不注册且磁盘内容未被修改
- [x] 7.6 核对目录 v3 迁移备份和回滚演练，确认恢复 v2 备份后旧版仍可读取原项目与手工分组
- [x] 7.7 生成 Windows NSIS 与 Portable 产物，记录文件名、大小和 SHA-256，并在安装版与便携版各完成一次 Codex 工作区导入冒烟测试
