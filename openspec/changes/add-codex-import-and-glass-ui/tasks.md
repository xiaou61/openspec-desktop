## 1. 共享契约与测试样本

- [x] 1.1 在共享契约中定义 Codex 候选状态、候选列表、扫描摘要、批量导入请求和逐项结果，并用 Zod 限制路径数量与字段长度
- [x] 1.2 为正常、多根、重复、损坏、缺字段和超限的 Codex 全局项目索引建立最小化测试样本，确保样本不含真实提示词、对话或凭据
- [x] 1.3 扩展桌面 API 与 IPC 通道类型，使列出候选和批量导入的输入输出在主进程、preload 与渲染进程之间一致

## 2. Codex 项目发现

- [x] 2.1 实现 `CODEX_HOME` 优先、用户目录 `.codex` 回退的 Codex 根目录解析，并处理目录不存在或不可读
- [x] 2.2 实现标准全局状态文件的有界读取、并发写入有限重试、`.bak` 只读回退和临时文件忽略
- [x] 2.3 宽松解析 `local-projects`、`project-order` 和 `electron-saved-workspace-roots`，支持不透明项目 ID 与多根目录且不返回其他原始字段
- [x] 2.4 按项目顺序展开并补充已保存工作区，规范化和去重候选路径，复用 OpenSpec 校验并标记导入状态
- [x] 2.5 为根目录回退、多根排序、Windows 扩展路径去重、候选截断、备份降级、损坏文件和隐私字段隔离补充主进程单元测试

## 3. 主进程、IPC 与 preload 集成

- [x] 3.1 在应用控制器中加入列出 Codex 候选能力，并把当前目录中的规范化项目路径传入发现服务
- [x] 3.2 复用现有项目注册流程实现串行批量导入，确认时重新校验路径，并返回保留部分成功的逐项结果与最新快照
- [x] 3.3 注册并校验 Codex 列表、导入 IPC 路由，在 preload 中只暴露最小化方法，不增加任意文件读取能力
- [x] 3.4 扩展 IPC、控制器和 preload 测试，覆盖未知通道、非法负载、重复项目、部分失败及渲染边界

## 4. Codex 导入交互

- [x] 4.1 将侧边栏添加按钮改为 Radix 菜单，提供“选择文件夹”和“从 Codex 导入”，保留原有目录选择流程
- [x] 4.2 使用 Radix Dialog 实现 Codex 导入弹窗，接入 React Query 加载与刷新候选，并提供标题、说明和焦点管理
- [x] 4.3 实现候选列表的单选、全选可用项、取消全选、已选数量、禁用原因、最近使用时间和扫描截断提示
- [x] 4.4 接入批量导入 mutation，处理提交禁用、全部成功、部分成功、全部失败和重试，成功后刷新并选中导入项目
- [x] 4.5 为添加菜单、弹窗开关、空态、错误态、刷新、选择逻辑和批量结果补充 React 组件测试

## 5. Vercel 式玻璃视觉系统

- [x] 5.1 整理根级颜色、表面、边框、阴影、圆角、间距、控件高度、字体和过渡令牌，建立中性黑白与语义状态色体系
- [x] 5.2 统一按钮、图标按钮、菜单、输入框、分段控件、状态徽标、进度条、列表行和 Tooltip 的默认及交互状态
- [x] 5.3 重做应用框架、侧边栏、Change 列表、详情工具栏和标签页样式，维持稳定的三栏高密度工作区
- [x] 5.4 为菜单、粘性工具栏和模态加入克制的半透明、背景模糊、细高光与轻阴影，并提供无模糊实色降级
- [x] 5.5 精修 Codex 导入弹窗的候选行、复选框、状态信息、空态、加载态、错误态和固定操作区
- [x] 5.6 处理 920 像素最小窗口、长项目名、Change 名称和文件路径，确保换行或省略后不遮挡相邻内容
- [x] 5.7 完成键盘焦点、Tooltip、状态非颜色表达、WCAG AA 对比和 `prefers-reduced-motion` 降级

## 6. 验证与 Windows 分发

- [x] 6.1 新增跨文件导出后强制刷新 CodeGraph 索引，并用定向查询核对 Codex 导入调用链和权限边界
- [x] 6.2 运行格式检查、TypeScript 类型检查、ESLint、单元测试、渲染边界检查和生产构建并修复全部回归
- [x] 6.3 用 Playwright Chromium 验证空目录和多项目工作区的添加菜单、Codex 导入与三栏布局
- [x] 6.4 用 Electron E2E 在 1440×900 和 920×700 窗口检查真实桌面交互、截图、非空像素及无重叠表现
- [x] 6.5 重新生成 Windows NSIS 安装版和 Portable 便携版，并记录产物名称、大小和 SHA-256

## 7. GitHub 交付

- [x] 7.1 核对 `.gitignore` 和待提交文件，排除 Codex 本机数据、测试临时目录、依赖、构建输出和发布产物
- [ ] 7.2 初始化 Git 仓库，创建包含源代码与中文 OpenSpec 工件的首个提交，确认提交中不存在凭据或个人会话内容
- [ ] 7.3 使用当前已登录账户创建或连接 `xiaou61/openspec-desktop` 私有仓库，推送默认分支并核对远端提交
